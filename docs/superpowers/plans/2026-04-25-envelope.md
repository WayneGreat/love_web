# 信封组件实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现经典信封组件，含三角翻盖、蜡封、信纸滑出动画，集成到时间轴末尾作为最后一个 snap 区块。

**Architecture:** LetterEnvelope 是单一组件，内部管理 `isOpen` 状态，用 Framer Motion variants + delay 编排翻盖翻转 → 信纸滑出的顺序动画。纯 CSS（clip-path + 渐变）绘制信封外观，从 config.letter 读取信件内容。集成到 Timeline.tsx 滚动容器末尾，复用 wrapper div + snap-start 模式。

**Tech Stack:** React 19, framer-motion 12, Tailwind CSS v4, Vite 8

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/components/LetterEnvelope.tsx` | 新建 | 信封组件：CSS 外观 + 开/关动画 + 蜡封点击 + 关闭按钮 |
| `src/components/Timeline.tsx` | 修改 | 末尾追加 LetterEnvelope 作为最后一个 snap 区块 |
| `src/components/TimelineNav.tsx` | 不变 | total 通过 Timeline 传参自动 +1 |
| `src/config.ts` | 不变 | 只读 letter 数据 |

---

### Task 1: 创建 LetterEnvelope 组件

**Files:**
- Create: `src/components/LetterEnvelope.tsx`

- [ ] **Step 1: 创建 LetterEnvelope.tsx**

```tsx
import { useEffect, useRef, useState } from "react";
import { motion, useInView } from "framer-motion";
import config from "../config";

const flapVariants = {
    closed: {
        rotateX: 0,
        transition: { duration: 0.6, ease: "easeIn" as const, delay: 0.3 },
    },
    open: {
        rotateX: 180,
        transition: { duration: 0.6, ease: "easeOut" as const },
    },
};

const letterVariants = {
    closed: {
        y: 0,
        opacity: 0,
        transition: { duration: 0.4, ease: "easeIn" as const, delay: 0.1 },
    },
    open: {
        y: -120,
        opacity: 1,
        transition: { duration: 0.5, ease: "easeOut" as const, delay: 0.4 },
    },
};

const textContainerVariants = {
    closed: {
        opacity: 1,
        transition: { duration: 0.2 },
    },
    open: {
        opacity: 1,
        transition: { staggerChildren: 0.05, delayChildren: 0.6 },
    },
};

const textLineVariants = {
    closed: { opacity: 0 },
    open: {
        opacity: 1,
        transition: { duration: 0.3 },
    },
};

interface LetterEnvelopeProps {
    index: number;
    onInView: (index: number) => void;
}

function LetterEnvelope({ index, onInView }: LetterEnvelopeProps) {
    const [isOpen, setIsOpen] = useState(false);
    const ref = useRef<HTMLDivElement>(null);
    const isInView = useInView(ref, { amount: 0.5 });

    useEffect(() => {
        if (isInView) onInView(index);
    }, [isInView, index, onInView]);

    return (
        <motion.div
            ref={ref}
            className="h-screen flex items-center justify-center px-6"
            initial={{ opacity: 0 }}
            animate={isInView ? { opacity: 1 } : { opacity: 0 }}
            transition={{ duration: 0.6 }}
        >
            <div className="relative" style={{ perspective: 800 }}>
                {/* Envelope body */}
                <div className="w-80 h-56 sm:w-96 sm:h-64 bg-gradient-to-b from-rose-100 to-rose-200 rounded-lg shadow-xl relative overflow-visible">
                    {/* Flap */}
                    <motion.div
                        className="absolute -top-[80px] sm:-top-[96px] left-0 right-0 h-[80px] sm:h-[96px] bg-gradient-to-b from-rose-300 to-rose-200 rounded-t-lg z-10"
                        style={{
                            clipPath: "polygon(0 100%, 50% 0, 100% 100%)",
                            transformOrigin: "top",
                        }}
                        variants={flapVariants}
                        animate={isOpen ? "open" : "closed"}
                    />

                    {/* Seal */}
                    <button
                        onClick={() => setIsOpen(true)}
                        className="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 w-8 h-8 rounded-full bg-rose-600 z-20 cursor-pointer shadow-md focus-visible:ring-2 focus-visible:ring-rose-400 focus-visible:outline-none"
                        aria-label="Open envelope"
                    >
                        <span className="block w-3 h-3 mx-auto mt-0.5 rounded-full border-2 border-rose-300" />
                    </button>

                    {/* Letter */}
                    <motion.div
                        className="absolute left-2.5 right-2.5 top-4 bottom-2 bg-white rounded-md shadow-inner p-5 pt-8 overflow-y-auto max-h-[200px] z-0"
                        variants={letterVariants}
                        animate={isOpen ? "open" : "closed"}
                    >
                        {/* Close button */}
                        <button
                            onClick={() => setIsOpen(false)}
                            className="absolute top-1.5 right-1.5 w-6 h-6 rounded-full bg-rose-100 hover:bg-rose-200 flex items-center justify-center text-rose-500 text-xs font-bold focus-visible:ring-2 focus-visible:ring-rose-400 focus-visible:outline-none"
                            aria-label="Close envelope"
                        >
                            ✕
                        </button>

                        {/* Letter text */}
                        <motion.div
                            className="font-handwriting text-rose-700 text-sm sm:text-base leading-relaxed whitespace-pre-line"
                            variants={textContainerVariants}
                            animate={isOpen ? "open" : "closed"}
                        >
                            {config.letter.split("\n").map((line, i) => (
                                <motion.p key={i} variants={textLineVariants}>
                                    {line || " "}
                                </motion.p>
                            ))}
                        </motion.div>
                    </motion.div>
                </div>
            </div>
        </motion.div>
    );
}

export default LetterEnvelope;
```

`★ Insight ─────────────────────────────────────`
- `perspective: 800` 设在信封体父级，让翻盖 `rotateX` 产生近大远小的 3D 透视效果。`transformOrigin: "top"` 保证翻盖从顶边翻转，模拟真实信封开合
- 关闭动画的 delay 逻辑：翻盖 delay 0.3s（等信纸先收回）、信纸 delay 0.1s（等文字先消失），形成逆序编排
- `clipPath: "polygon(0 100%, 50% 0, 100% 100%)"` 裁剪出等腰三角形翻盖，底边 100% 对齐信封体顶部
- 蜡封用 CSS 圆形 + 内嵌小圆环模拟蜡封纹路，无需图片
- 信纸用 `motion.div` + variants 同时控制 translateY 和 opacity，封闭状态下 opacity: 0 避免闪现
`─────────────────────────────────────────────────`

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 验证构建**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 4: 提交**

```bash
git add src/components/LetterEnvelope.tsx
git commit -m "feat: add LetterEnvelope with flap animation and letter reveal"
```

---

### Task 2: 集成 LetterEnvelope 到 Timeline

**Files:**
- Modify: `src/components/Timeline.tsx`

当前 Timeline.tsx 内容：

```tsx
import { useCallback, useRef, useState } from "react";
import config from "../config";
import TimelineSection from "./TimelineSection";
import TimelineNav from "./TimelineNav";

function Timeline() {
    const [activeIndex, setActiveIndex] = useState(0);
    const sectionRefs = useRef<(HTMLDivElement | null)[]>([]);

    const handleInView = useCallback((index: number) => {
        setActiveIndex(index);
    }, []);

    const handleNavigate = useCallback((index: number) => {
        const el = sectionRefs.current[index];
        if (el) {
            el.scrollIntoView({ behavior: "smooth", block: "start" });
        }
    }, []);

    return (
        <>
            <div className="h-screen overflow-y-auto snap-y snap-mandatory">
                {config.timeline.map((entry, i) => (
                    <div
                        key={entry.date}
                        ref={(el) => { sectionRefs.current[i] = el; }}
                        className="snap-start"
                    >
                        <TimelineSection
                            entry={entry}
                            index={i}
                            onInView={handleInView}
                        />
                    </div>
                ))}
            </div>
            <TimelineNav
                activeIndex={activeIndex}
                total={config.timeline.length}
                onNavigate={handleNavigate}
            />
        </>
    );
}

export default Timeline;
```

- [ ] **Step 1: 修改 Timeline.tsx — 追加信封区块并更新总数**

将 Timeline.tsx 替换为：

```tsx
import { useCallback, useRef, useState } from "react";
import config from "../config";
import TimelineSection from "./TimelineSection";
import TimelineNav from "./TimelineNav";
import LetterEnvelope from "./LetterEnvelope";

function Timeline() {
    const totalSections = config.timeline.length + 1;
    const [activeIndex, setActiveIndex] = useState(0);
    const sectionRefs = useRef<(HTMLDivElement | null)[]>([]);

    const handleInView = useCallback((index: number) => {
        setActiveIndex(index);
    }, []);

    const handleNavigate = useCallback((index: number) => {
        const el = sectionRefs.current[index];
        if (el) {
            el.scrollIntoView({ behavior: "smooth", block: "start" });
        }
    }, []);

    return (
        <>
            <div className="h-screen overflow-y-auto snap-y snap-mandatory">
                {config.timeline.map((entry, i) => (
                    <div
                        key={entry.date}
                        ref={(el) => { sectionRefs.current[i] = el; }}
                        className="snap-start"
                    >
                        <TimelineSection
                            entry={entry}
                            index={i}
                            onInView={handleInView}
                        />
                    </div>
                ))}
                <div
                    ref={(el) => { sectionRefs.current[config.timeline.length] = el; }}
                    className="snap-start"
                >
                    <LetterEnvelope
                        index={config.timeline.length}
                        onInView={handleInView}
                    />
                </div>
            </div>
            <TimelineNav
                activeIndex={activeIndex}
                total={totalSections}
                onNavigate={handleNavigate}
            />
        </>
    );
}

export default Timeline;
```

`★ Insight ─────────────────────────────────────`
- `totalSections = config.timeline.length + 1` 让 TimelineNav 多一个圆点代表信封区块，用户可通过导航跳转
- 信封 wrapper div 复用 `snap-start` + ref 模式，与时间轴条目一致，滚动吸附和导航跳转自动生效
- LetterEnvelope 接收 `index` + `onInView`，用 `useInView` 检测可见性并上报，与 TimelineSection 模式一致，导航圆点自动同步高亮
`─────────────────────────────────────────────────`

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 验证构建**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 4: 提交**

```bash
git add src/components/Timeline.tsx
git commit -m "feat: integrate LetterEnvelope into Timeline as last snap section"
```

---

### Task 3: 构建验证与收尾

**Files:** 无变更

- [ ] **Step 1: 完整构建验证**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 2: TypeScript 严格检查**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 确认 dist 路径前缀**

Run: `find dist -name '*.html' -o -name '*.js' | head -5 | xargs grep -l '/love_web/'`
Expected: dist 中的资源路径包含 `/love_web/` 前缀

- [ ] **Step 4: 检查 git 状态**

Run: `git status`
Expected: 工作区干净，所有变更已提交
