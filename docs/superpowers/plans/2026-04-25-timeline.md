# 时间轴组件实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现全屏分段滚动的时间轴组件，包含卡片进入动画和侧边导航圆点。

**Architecture:** TimelineSection 是纯展示组件，接收 `entry` 数据 + `onInView` 回调，用 `whileInView` 做动画、`useInView` 报告可见性。Timeline 是容器组件，持有 `activeIndex` 状态，管理滚动吸附和导航同步。TimelineNav 是右侧固定圆点导航，接收 `activeIndex` + `onNavigate`。App.tsx 用 Timeline 替换现有验证内容。

**Tech Stack:** React 19, framer-motion 12, Tailwind CSS v4, Vite 8

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/components/TimelineSection.tsx` | 新建 | 单条时间点卡片，动画 + 可见性上报 |
| `src/components/TimelineNav.tsx` | 新建 | 侧边导航圆点 |
| `src/components/Timeline.tsx` | 新建 | 时间轴容器 + activeIndex 状态 + 滚动吸附 |
| `src/App.tsx` | 修改 | 集成 Timeline，移除验证内容 |
| `src/config.ts` | 不变 | 只读 timeline 数据 |

---

### Task 1: 创建 TimelineSection 组件

**Files:**
- Create: `src/components/TimelineSection.tsx`

- [ ] **Step 1: 创建 TimelineSection.tsx**

```tsx
import { useEffect, useRef } from "react";
import { motion, useInView } from "framer-motion";
import type { TimelineEntry } from "../config";

interface TimelineSectionProps {
    entry: TimelineEntry;
    index: number;
    onInView: (index: number) => void;
}

const containerVariants = {
    hidden: {},
    visible: {
        transition: {
            staggerChildren: 0.15,
        },
    },
};

const itemVariants = {
    hidden: { opacity: 0, y: 30 },
    visible: {
        opacity: 1,
        y: 0,
        transition: { duration: 0.5, ease: "easeOut" },
    },
};

const imageVariants = {
    hidden: { opacity: 0, y: 30, scale: 0.9 },
    visible: {
        opacity: 1,
        y: 0,
        scale: 1,
        transition: { duration: 0.5, ease: "easeOut" },
    },
};

function TimelineSection({ entry, index, onInView }: TimelineSectionProps) {
    const ref = useRef<HTMLElement>(null);
    const isInView = useInView(ref, { amount: 0.5 });

    useEffect(() => {
        if (isInView) onInView(index);
    }, [isInView, index, onInView]);

    return (
        <motion.section
            ref={ref}
            className="h-screen snap-start flex items-center justify-center px-6"
            variants={containerVariants}
            initial="hidden"
            whileInView="visible"
            viewport={{ amount: 0.5 }}
        >
            <div className="max-w-5xl w-full flex flex-col md:flex-row items-center gap-8 md:gap-12">
                <motion.div
                    className="flex-1 text-center md:text-left"
                    variants={containerVariants}
                >
                    <motion.p
                        className="text-sm text-rose-300 mb-2"
                        variants={itemVariants}
                    >
                        {entry.date}
                    </motion.p>
                    <motion.h2
                        className="text-3xl font-handwriting text-rose-600 mb-4"
                        variants={itemVariants}
                    >
                        {entry.title}
                    </motion.h2>
                    <motion.p
                        className="text-rose-400 leading-relaxed"
                        variants={itemVariants}
                    >
                        {entry.desc}
                    </motion.p>
                </motion.div>
                <motion.div
                    className="flex-1 w-full"
                    variants={imageVariants}
                >
                    <img
                        src={entry.image}
                        alt={entry.title}
                        className="w-full max-h-[60vh] object-cover rounded-xl shadow-lg"
                    />
                </motion.div>
            </div>
        </motion.section>
    );
}

export default TimelineSection;
```

`★ Insight ─────────────────────────────────────`
- `useInView` hook 和 `whileInView` prop 并存但职责不同：hook 用于上报可见性状态给父组件（驱动导航高亮），prop 用于声明式动画触发。两者都用 `amount: 0.5` 保持一致
- `variants` + `staggerChildren: 0.15` 让子元素按序进入：日期 → 标题 → 描述 → 图片，每个延迟 150ms
- 图片单独用 `imageVariants`（额外 `scale: 0.9 → 1`）增加视觉层次感
`─────────────────────────────────────────────────`

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 验证构建**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 4: 提交**

```bash
git add src/components/TimelineSection.tsx
git commit -m "feat: add TimelineSection with staggered entrance animations"
```

---

### Task 2: 创建 TimelineNav 组件

**Files:**
- Create: `src/components/TimelineNav.tsx`

- [ ] **Step 1: 创建 TimelineNav.tsx**

```tsx
import { motion } from "framer-motion";

interface TimelineNavProps {
    activeIndex: number;
    total: number;
    onNavigate: (index: number) => void;
}

function TimelineNav({ activeIndex, total, onNavigate }: TimelineNavProps) {
    return (
        <nav className="fixed right-4 top-1/2 -translate-y-1/2 z-20 flex flex-col gap-4">
            {Array.from({ length: total }, (_, i) => (
                <button
                    key={i}
                    onClick={() => onNavigate(i)}
                    className="p-1 outline-none"
                    aria-label={`Go to section ${i + 1}`}
                >
                    <motion.span
                        className="block w-3 h-3 rounded-full"
                        layout
                        animate={
                            i === activeIndex
                                ? { scale: 1.25, backgroundColor: "#e11d48" }
                                : { scale: 1, backgroundColor: "rgba(253,164,175,0.5)" }
                        }
                        transition={{ type: "spring", stiffness: 300, damping: 25 }}
                    />
                </button>
            ))}
        </nav>
    );
}

export default TimelineNav;
```

`★ Insight ─────────────────────────────────────`
- `layout` prop 让圆点尺寸变化时 Framer Motion 自动做布局动画，高亮切换更流畅
- `type: "spring"` 弹簧物理动画比 `ease` 更有弹性手感，`stiffness: 300, damping: 25` 是轻快回弹的经典参数
- `#e11d48` 是 Tailwind rose-600 的原始值，直接使用以匹配 `animate` 的对象语法
- `p-1` 增大点击热区，`outline-none` 移除默认 focus 样式（后续可加自定义 focus ring）
`─────────────────────────────────────────────────`

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 验证构建**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 4: 提交**

```bash
git add src/components/TimelineNav.tsx
git commit -m "feat: add TimelineNav with animated dot indicators"
```

---

### Task 3: 创建 Timeline 容器组件

**Files:**
- Create: `src/components/Timeline.tsx`

- [ ] **Step 1: 创建 Timeline.tsx**

```tsx
import { useCallback, useRef, useState } from "react";
import config from "../config";
import TimelineSection from "./TimelineSection";
import TimelineNav from "./TimelineNav";

function Timeline() {
    const [activeIndex, setActiveIndex] = useState(0);
    const sectionRefs = useRef<(HTMLDivElement | null)[]>([]);

    const handleInView = useCallback((index: number) => {
        setActiveIndex(index);
    }, []);

    const handleNavigate = useCallback((index: number) => {
        const el = sectionRefs.current[index];
        if (el) {
            el.scrollIntoView({ behavior: "smooth", block: "start" });
        }
    }, []);

    return (
        <>
            <div className="h-screen overflow-y-auto snap-y snap-mandatory">
                {config.timeline.map((entry, i) => (
                    <div
                        key={entry.date}
                        ref={(el) => { sectionRefs.current[i] = el; }}
                    >
                        <TimelineSection
                            entry={entry}
                            index={i}
                            onInView={handleInView}
                        />
                    </div>
                ))}
            </div>
            <TimelineNav
                activeIndex={activeIndex}
                total={config.timeline.length}
                onNavigate={handleNavigate}
            />
        </>
    );
}

export default Timeline;
```

`★ Insight ─────────────────────────────────────`
- 外层 `<div ref={...}>` 包裹 TimelineSection 是因为 `scrollIntoView` 需要一个 DOM ref，而 TimelineSection 用 `useInView` 占用了自己的 ref。用 wrapper div 分离关注点
- `snap-y snap-mandatory`（Tailwind v4 语法）等价于 `scroll-snap-type: y mandatory`，`snap-start` 等价于 `scroll-snap-align: start`
- `handleInView` 和 `handleNavigate` 用 `useCallback` 避免每次渲染创建新函数导致子组件不必要的重渲染
- TimelineNav 渲染在 scroll container 外部，所以它的 `fixed` 定位不受滚动影响
`─────────────────────────────────────────────────`

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 验证构建**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 4: 提交**

```bash
git add src/components/Timeline.tsx
git commit -m "feat: add Timeline container with scroll-snap and navigation sync"
```

---

### Task 4: 集成 Timeline 到 App 布局

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: 更新 App.tsx**

将现有 App.tsx 内容替换为：

```tsx
import ParticleBackground from "./components/ParticleBackground";
import Timeline from "./components/Timeline";

function App() {
    return (
        <div className="min-h-screen relative">
            <ParticleBackground />
            <div className="relative z-10">
                <Timeline />
            </div>
        </div>
    );
}

export default App;
```

`★ Insight ─────────────────────────────────────`
- 移除了 `import config` 和验证用的 `config.timeline[0]` 硬编码，Timeline 组件内部自行读取 config
- Timeline 内部已渲染 TimelineNav（z-20），App 只需放 Timeline 即可
- ParticleBackground（z-0）+ 内容层（z-10）的分层结构保持不变
`─────────────────────────────────────────────────`

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 验证构建**

Run: `npm run build`
Expected: 构建成功，dist 输出正常

- [ ] **Step 4: 启动开发服务器做视觉验证**

Run: `npm run dev`
Expected: 浏览器中页面有粉色渐变背景+爱心粒子，4 个时间点卡片全屏分段展示，滚动吸附，卡片元素 stagger 进入动画，右侧导航圆点高亮跟随

- [ ] **Step 5: 提交**

```bash
git add src/App.tsx
git commit -m "feat: integrate Timeline into App layout, replace verification content"
```

---

### Task 5: 构建验证与收尾

**Files:** 无变更

- [ ] **Step 1: 完整构建验证**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 2: TypeScript 严格检查**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 确认 dist 路径前缀**

Run: `grep -r '/love_web/' dist/`
Expected: dist 中的资源路径包含 `/love_web/` 前缀

- [ ] **Step 4: 检查 git 状态**

Run: `git status`
Expected: 工作区干净，所有变更已提交
